<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="csrf-token" content={get_csrf_token()} />
    <link rel="icon" type="image/svg+xml" href={~p"/images/logo-mark.svg"} />
    <%!-- Type pairing: Syne 700/800 (display), Space Grotesk 400/500/600 (body),
         JetBrains Mono 400/500 (chain IDs / tickers). Bunny Fonts is a privacy-
         preserving Google Fonts mirror — same CSS, no Google IP exposure. --%>
    <link rel="preconnect" href="https://fonts.bunny.net" crossorigin />
    <link
      rel="stylesheet"
      href="https://fonts.bunny.net/css?family=syne:700,800|space-grotesk:400,500,600|jetbrains-mono:400,500&display=swap"
    />
    <.live_title>
      {assigns[:page_title] || "Kite Agent Hub"}
    </.live_title>
    <script>
      (() => {
        const media = window.matchMedia("(prefers-color-scheme: dark)");
        const systemTheme = () => media.matches ? "dark" : "light";
        const setTheme = (theme) => {
          const mode = theme || "system";
          const resolved = mode === "system" ? systemTheme() : mode;

          if (mode === "system") {
            localStorage.removeItem("phx:theme");
          } else {
            localStorage.setItem("phx:theme", mode);
          }

          document.documentElement.setAttribute("data-theme", resolved);
          document.documentElement.setAttribute("data-theme-mode", mode);
        };
        setTheme(localStorage.getItem("phx:theme") || "system");
        window.addEventListener("storage", (e) => e.key === "phx:theme" && setTheme(e.newValue || "system"));
        media.addEventListener("change", () => {
          if (!localStorage.getItem("phx:theme")) setTheme("system");
        });
        window.addEventListener("phx:set-theme", (e) => {
          const button = e.target.closest("[data-phx-theme]");
          if (button) setTheme(button.dataset.phxTheme);
        });
      })();
    </script>
    <link phx-track-static rel="stylesheet" href={~p"/assets/css/app.css"} />
    <script defer phx-track-static type="text/javascript" src={~p"/assets/js/app.js"}>
    </script>
  </head>
  <body class="kah-theme-body antialiased selection:bg-[#22c55e]/30">
    {@inner_content}
  </body>
</html>
